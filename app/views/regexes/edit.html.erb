<h2>Edit Regex</h2>
Here is the place for you to modify <b> <%= @regex.title %> </b> (expression <b>/<%= @regex.expression %>/</b>) - with rules!


<ul id="Edit hints">
  <li>
  Name and expression can not be changed.
  </li>
  <li>
    Add new testcase in the homepage of regex (click on cancel and go back), not here.
  </li>
</ul>

<%= form_for @regex, url: regex_path(@regex), method: "patch" do |f| %>
  <%= f.label :description, 'Description (Optional)', :class => 'col-form-label' %>
  <%= f.text_field :description, :class => 'form-control' %>
  <%= f.label :tag, 'Tags (Optional, use comma to separate multiple tags)', :class => 'col-form-label' %>
  <%= f.text_field :tag, :class => 'form-control' %>
  <br/>
<br/>
  <%= f.submit 'Save Changes', :class => 'btn btn-success' %>
  <%= link_to 'Cancel', regexes_path, :class => 'btn btn-secondary' %>
<% end %>