<h2>All Regexes</h2>

<%= form_tag regexes_path, method: :get, enforce_utf8: false, id: 'tags_form' do %>
    Include:
    <% @all_tags.each do |tag| %>
      <div class="form-check  form-check-inline">
        <%= label_tag "tags[#{tag}]", tag, class: 'form-check-label' %>
        <%= check_box_tag "tags[#{tag}]", "1",  @tags_to_show.include?(tag), :id => "tags_#{tag}", class: 'form-check-input' %>
      </div>
    <% end %>
    <%= submit_tag 'Refresh', id: 'tags_submit', class: 'btn btn-primary' %>
<% end %>

<table class="table table-striped col-md-12" id="regexes">
  <thead>
    <tr>
      <th>Name</th>
      <th>Expression</th>
      <th>Uploaded Time</th>
      <th>Tag</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <% @regexes.each do |regex| %>
      <tr>
        <td>
          <%= link_to regex.title, regex_path(regex) %>
        </td>
        <td>
          <%= regex.expression %>
        </td>
        <td>
          <%= regex.created_at %>
        </td>
        <td>
          <%= regex.tag %>
        </td>
        <td>
          <%= regex.description %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Add new regular expression', new_regex_path, :class => 'btn btn-primary' %>
